{
  "name": "mimirlink-monorepo",
  "private": true,
  "version": "0.12.0",
  "workspaces": [
    "src/frontend",
    "src/backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=mimirlink",
    "dev:backend": "npm run dev --workspace=mimirlink-backend",
    "build:frontend": "npm run build --workspace=mimirlink",
    "build:backend": "npm run build --workspace=mimirlink-backend",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.2"
  },
  "dependencies": {
    "@lezer/markdown": "^1.6.0"
  }
}
